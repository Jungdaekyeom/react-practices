import React, {Fragment} from 'react';
import Clock from './Clock'

export default function() {    
    return (
        <Fragment>
            <p>{ `08. Comment` }</p>
            <Clock />
        </Fragment>
    );
}
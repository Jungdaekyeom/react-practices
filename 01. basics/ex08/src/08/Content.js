import React, {Fragment} from 'react';
import Clock from './Clock'

export default function() {    
    return (
        <Fragment>
            <p>{ `07. Dynamic HTML Rendering` }</p>
            <Clock />
        </Fragment>
    );
}